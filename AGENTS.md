# Project instructions

<!-- BEGIN MANAGED: codex-default-specialist -->
## Default Codex specialist: `peermetrics`

Use `peermetrics` as this project's default operating profile. Before substantive work, read `.codex/agents/peermetrics.toml` and apply its `developer_instructions`. Read `.codex/context/agents/peermetrics.md` when durable continuity is relevant, and never load another specialist's context unless the current task requires a handoff.

Read `.codex/project-workspace.json` before resolving the work target. For repository hubs, coordination hubs, and content hubs, follow its target policy and do not assume every implementation asset lives at the project root.

The current user request and instructions nearer the working directory are authoritative. Treat durable context as background only: do not revive stale priorities, assume that a product checkout or runtime is present, or claim implementation or QA evidence until the real target has been resolved.

When parallel specialist work is useful, delegate it to the named `peermetrics` custom agent. The managed profile and context are synchronized from `/Users/alberto/Documents/ChatGPT/openclaw-to-codex-migration`.
<!-- END MANAGED: codex-default-specialist -->
